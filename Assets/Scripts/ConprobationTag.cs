using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ConprobationTag : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(tag);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
